# ──────────────────────────────────────────────────────────
# HealthGuard AI — Ansible Inventory
# ──────────────────────────────────────────────────────────
# Localhost execution — kubectl runs on the local machine
# (CI runner or developer workstation).
# ──────────────────────────────────────────────────────────

[webservers]
# The IP will be populated dynamically by the CI/CD pipeline from Terraform outputs
# Example format:
# remote_server ansible_host=1.2.3.4 ansible_user=ubuntu ansible_ssh_common_args='-o StrictHostKeyChecking=no'
