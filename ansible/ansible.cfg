# ──────────────────────────────────────────────────────────
# HealthGuard AI — Ansible Configuration
# ──────────────────────────────────────────────────────────

[defaults]
inventory = inventory.ini
host_key_checking = False
deprecation_warnings = False
retry_files_enabled = False
stdout_callback = yaml

[privilege_escalation]
become = False
